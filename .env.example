# Copy to .env and adjust values.
# Vite exposes variables prefixed with VITE_ via import.meta.env

# Base URL for backend API (leave blank to use same-origin)
VITE_API_BASE_URL=http://localhost:3000
